#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
 int a, b;
 int x;
 printf("输入两个整数：\n");
 scanf("%d%d", &a, &b);
 while (a%b)
{
	x = a % b;
	a = b;
	b = x;
}
 printf("输入的两个整数的最大公约数为 %d\n", x);
 return 0;
}

