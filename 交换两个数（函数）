#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
void swap(int *a,int *b )
{
	int x = 0;
	x = *a;
	*a = *b;
	*b = x;
	}
int main()
{
	int x = 0;
	int y = 0;
	printf("请输入两个数\n");
	scanf("%d %d", &x,&y);
	swap(&x, &y);
	printf("x=%d y=%d\n", x, y);
	return 0;
		}
